<% if flash[:success].present? %>
  $('#edu-group-tab-<%= @group_object.id %>').html('<%= j render partial: "users", locals: {users: @group_object.users, group: @group_object} %>')
  $.growl.notice({message: '<%= flash[:success] %>'});
<% else %>
  $.growl.error({message: '<%= flash[:danger] %>'});
<% end %>
